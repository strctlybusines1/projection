# Run NHL DFS Pipeline
1. cd to the NHL DFS project directory
2. Ensure the virtual environment is activated
3. Set `export KMP_DUPLICATE_LIB_OK=TRUE`
4. Run `python main.py`
5. Report the number of lineups exported
6. Ask if user wants to git add, commit, and push results
